import React from 'react';
import {FilterLocalView} from '../components/views/FilterLocalView'

export default function FilterLocal({navigation}){
    
    return(<FilterLocalView navigation={navigation}>

        </FilterLocalView>);
}