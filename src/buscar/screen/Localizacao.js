import React from 'react';
import {LocalizacaoView} from '../components/views/LocalizacaoView'

export default function Localizacao({navigation}){
    
    return(<LocalizacaoView navigation={navigation}>

        </LocalizacaoView>);
}