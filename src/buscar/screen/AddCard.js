import React from 'react';
import {AddCardView} from '../components/views/AddCardView'

export default function AddCard({navigation}){
    
    return(<AddCardView navigation={navigation}>

        </AddCardView>);
}